# ==========================================
# HR Leave Management System - Backend Configuration
# Version: 2.0.0
# ==========================================
# Copy this file to .env and update the values
# ==========================================

# Server Configuration
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Database Configuration
# Update these values to match your SQL Server setup
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=HRLeaveSystemDB
DB_USER=hrapp_user
DB_PASSWORD=your_secure_password_here
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# JWT Authentication
# Generate a strong random secret key (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_random_secret_key_here_minimum_32_characters
JWT_EXPIRES_IN=24h

# Logging Configuration
LOG_LEVEL=info
LOG_FILE_ERROR=./logs/error.log
LOG_FILE_COMBINED=./logs/combined.log

# CORS Configuration
# For local network access, use * or specify allowed origins
CORS_ORIGIN=*

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Login Rate Limiting
LOGIN_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX_ATTEMPTS=5

